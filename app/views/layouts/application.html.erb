<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Senior Rails Engineer Technical Assessment" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-slate-50 text-slate-900">
    <div class="min-h-screen">
      <header class="border-b border-slate-200 bg-white">
        <div class="mx-auto flex max-w-6xl items-center justify-between px-6 py-6">
          <div>
            <p class="text-sm font-semibold text-indigo-600">Campaign Dispatcher</p>
            <p class="text-lg font-semibold text-slate-900">Customer Feedback Automation</p>
          </div>
          <div class="text-xs text-slate-500">Rails 7 · Hotwire · Sidekiq</div>
        </div>
      </header>
      <main class="mx-auto max-w-6xl px-6 py-10">
        <% flash.each do |type, message| %>
          <% next if message.blank? %>
          <% color = type.to_s == "notice" ? "border-emerald-200 bg-emerald-50 text-emerald-700" : "border-rose-200 bg-rose-50 text-rose-700" %>
          <div class="mb-6 rounded-lg border px-4 py-3 text-sm <%= color %>">
            <%= message %>
          </div>
        <% end %>
        <%= yield %>
      </main>
    </div>
  </body>
</html>
